from setuptools import setup

setup(name="stdlib",
      version="2.0.1",
      description="A worse module ever made..",
      home_page="https://github.com/ForgettenAccount/stdlib/"
      author="ForgettenAccout",
      packages=["stdlib"],
      requires=['pycolor'],
      zip_safe=False)
