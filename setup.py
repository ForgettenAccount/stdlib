from setuptools import setup

setup(name="stdlib",
      version="2.0.1",
      description="A worse module ever made..",
      author="DuckTech",
      packages=["stdlib"],
      requires=['pycolor'],
      zip_safe=False)
